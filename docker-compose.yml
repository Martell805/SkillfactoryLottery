services:
  postgres:
    image: postgres:16
    container_name: sflottery-postgres
    environment:
      POSTGRES_DB: sflottery
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
